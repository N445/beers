{% extends 'base.html.twig' %}

{% block title %}Beer{% endblock %}

{% block body %}
    <div class="container">
        <ul>
            {% for beer in beers %}
                <li>{{ beer.name }} {{ beer.brewer.name }} {{ beer.brewer.country }}</li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
